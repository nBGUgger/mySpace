<!-- For prac for bg hunter -->
<?php
$username = $_POST['username'];
$password = $_POST['userPassword'];

if(isset(username) && !empty(username)){
  echo "the username is "+$username+"and the password is "+$password;
  
};
?>
